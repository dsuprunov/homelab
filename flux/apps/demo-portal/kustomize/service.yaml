apiVersion: v1
kind: Service
metadata:
  name: demo-portal
  namespace: demo-portal
spec:
  selector:
    app: demo-portal
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
