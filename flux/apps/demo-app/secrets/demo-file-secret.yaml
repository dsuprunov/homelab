# Reference template only.
# This file is not applied by Flux because secrets/kustomization.yaml uses resources: [].
apiVersion: v1
kind: Secret
metadata:
  name: demo-file-secret
  namespace: demo-app
type: Opaque
stringData:
  DEMO_FILE_SECRET_LOGIN: my-file-login
  DEMO_FILE_SECRET_PASSWORD: my-file-password
