# Reference template only.
# This file is not applied by Flux because secrets/kustomization.yaml uses resources: [].
apiVersion: v1
kind: Secret
metadata:
  name: demo-env-secret
  namespace: demo-app
type: Opaque
stringData:
  DEMO_ENV_SECRET_LOGIN: my-env-login
  DEMO_ENV_SECRET_PASSWORD: my-env-password
